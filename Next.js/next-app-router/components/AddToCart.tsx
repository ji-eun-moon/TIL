"use client";

import React from "react";

const AddToCart = () => {
  return (
    <button className="btn" onClick={() => console.log("AddToCart")}>
      Add
    </button>
  );
};

export default AddToCart;
